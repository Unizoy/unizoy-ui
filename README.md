## Build the content Layer using 

```bash
pnpm run build:docs
```

## There are two index file one in the __registry__ which is generated using **build:registry** cmd and anothere is  config/index.tsx which is handmade 
