---
title: Custom Pointer
description: Cursor changes on hover to card and moves with a name tag with random color each time.
links:
  doc: https://www.radix-ui.com/docs/primitives/components/accordion
---

<ComponentPreview 
  name="custom-pointer-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
/>

<Tabs defaultValue="manual">

<TabsList>
  <TabsTrigger value="manual">Manual</TabsTrigger>
  <TabsTrigger value="cli">CLI</TabsTrigger>
</TabsList>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install gsap
```
<Step>Make a file file for cn function and match the import afterwards</Step>

```tsx
import clsx, { ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'
export const cn = (...classes: ClassValue[]) => twMerge(clsx(...classes))
```

<Step>Make a file and copy paste this code in a file with name CustomCursor</Step>

<ComponentSource name="custom-pointer"/>

</Steps>

</TabsContent>

<TabsContent value="cli">

<Steps>

<Step>sdsd</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

### Custom Pointer With Custom Cursor

<ComponentPreview name={"custom-pointer-custom-cursor"}/>