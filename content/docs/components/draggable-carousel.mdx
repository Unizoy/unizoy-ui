---
title: Draggable Carousel
description: The DraggableCarousel component is a GSAP-powered animated carousel which has inertia.
---

<ComponentPreview
  name="draggable-carousel-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
/>

<Tabs defaultValue="manual">

<TabsList>
  <TabsTrigger value="manual">Manual</TabsTrigger>
  <TabsTrigger value="cli">CLI</TabsTrigger>
</TabsList>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install gsap @gsap/react
```
<Step>Make a file file for cn and mergerRef functions and match the import afterwards</Step>

```tsx
import clsx, { ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'
export const cn = (...classes: ClassValue[]) => twMerge(clsx(...classes))

```

<Step>Make a file and copy paste this code in a file with name `draggable-carousel.tsx`</Step>

<ComponentSource name="draggable-carousel"/>

</Steps>

</TabsContent>

<TabsContent value="cli">

<Steps>

<Step>Yet to come.</Step>

</Steps>

</TabsContent>

</Tabs>

### Props

| Name          | Type                                              | Description                                                       |
| ------------- | ------------------------------------------------- | ----------------------------------------------------------------- |
| `items`\*     | `Arrav<{title,description,imageUrl}>`             | Array of card in carousel   |
| `friction`    | `number`                                          | The value should be between 0-1, 1 for max smoothness |
| `cardWidth`   | `string`             | Width of each card in string format example:- `300px`   |

> **Note:** Props marked with `*` are required.
