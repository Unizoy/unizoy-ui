---
title: Product Preview
description: Specification's animation around your product
---

<ComponentPreview
  name="product-preview-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
/>

<Tabs defaultValue="manual">

<TabsList>
  <TabsTrigger value="manual">Manual</TabsTrigger>
  <TabsTrigger value="cli">CLI</TabsTrigger>
</TabsList>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install gsap @gsap/react
```
<Step>Make a file file for cn and mergerRef functions and match the import afterwards</Step>

```tsx
import clsx, { ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'
export const cn = (...classes: ClassValue[]) => twMerge(clsx(...classes))

```

<Step>Make a file and copy paste this code in a file with name `product-preview.tsx`</Step>

<ComponentSource name="product-preview"/>

</Steps>

</TabsContent>

<TabsContent value="cli">

<Steps>

<Step>Yet to come.</Step>

</Steps>

</TabsContent>

</Tabs>

### Props

| Name             | Type                                                      | Description                                                            |
| ---------------- | --------------------------------------------------------- | ---------------------------------------------------------------------- |
| `productSrc`*    | `string`                                                   | Source URL of the product image.                                        |
| `articleTop`*    | `{ <br> title: { text: string, className?: string }, description: { text: string, className?: string }, icon?: React.ReactNode }[]` | Array of article objects displayed at the top with title, description, and optional icon. |
| `articleBottom`* | `{ title: { text: string, className?: string }, description: { text: string, className?: string }, icon?: React.ReactNode }[]` | Array of article objects displayed at the bottom with title, description, and optional icon. |
| `className`      | `string`                                                   | Additional class names for styling the main container.                  |
| `productHeight`  | `number` (default: `298`)                                  | Height of the product image before scaling.                             |
| `productWidth`   | `number` (default: `1250`)                                 | Width of the product image before scaling.                              |
| `scaleFactor`    | `number` (default: `0.1`)                                  | Scale factor for resizing the product image.                            |
| `rotate`         | `number` (default: `30`)                                   | Rotation angle of the product image in degrees.                         |
| `length`         | `number` (default: `(articleTop.length * 2 - 1) * 50`)     | Length used in ScrollTrigger for defining the end point.                |
| `scroller`       | `RefObject<HTMLElement>`                                   | Scroll container reference for ScrollTrigger.                          |
